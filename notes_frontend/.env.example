# Frontend environment for Notes App (React)

# Preferred API base. Example for local dev:
# REACT_APP_API_BASE=http://localhost:4000/api

# Alternative legacy variable, still supported as a fallback:
# REACT_APP_BACKEND_URL=http://localhost:4000/api

# Optional: frontend origin (used primarily by backend CORS, not by the frontend itself)
# REACT_APP_FRONTEND_URL=http://localhost:3000

# Optional runtime flags
# REACT_APP_NODE_ENV=development
# REACT_APP_ENABLE_SOURCE_MAPS=true
# REACT_APP_NEXT_TELEMETRY_DISABLED=1
# REACT_APP_FEATURE_FLAGS=
# REACT_APP_EXPERIMENTS_ENABLED=
