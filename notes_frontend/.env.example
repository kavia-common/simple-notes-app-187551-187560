# Preferred: exact backend API base (no trailing slash)
# Example for local dev:
# REACT_APP_API_BASE=http://localhost:4000/api

# Example for cloud preview (host varies):
# REACT_APP_API_BASE=https://<host>:8000/api
